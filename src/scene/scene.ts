// scene/scene.ts
// Typed API for CarPlay connection lifecycle and interface controller operations.
// Wraps: connect/disconnect events, setRootTemplate, pushTemplate, popTemplate,
// popToRootTemplate, presentTemplate, dismissTemplate.
// May maintain cached connection state for synchronous reads by hooks.
// See: docs/carplay-api-surface.md §1 — Scene Lifecycle & Interface Controller
