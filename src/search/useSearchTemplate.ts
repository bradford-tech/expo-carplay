// search/useSearchTemplate.ts
// React hook for managing a CPSearchTemplate.
// Handles: template creation, search text change callbacks,
// result updates, and result selection events.
// See: docs/carplay-api-surface.md §6

export function useSearchTemplate() {
  return { templateId: null };
}
