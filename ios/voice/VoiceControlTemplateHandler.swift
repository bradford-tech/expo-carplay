// VoiceControlTemplateHandler.swift
// Manages CPVoiceControlTemplate and CPVoiceControlState.
// Handles: creating voice control templates with multiple states,
// switching the active state, and configuring action buttons (iOS 26.4+).
// See: docs/carplay-api-surface.md §15 — Voice Control Template
