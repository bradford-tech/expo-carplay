// ListItemConverter.swift
// Converts JS config dictionaries to CPListItem, CPListSection, CPListImageRowItem,
// and their various element styles (row, card, condensed, grid, image grid).
// Handles: CPListItemAccessoryType, item handlers, image sizing, and section headers.
// See: docs/carplay-api-surface.md §7 — List Template
