// ImageConverter.swift
// Cross-cutting utility for converting JS image references to UIImage and CPImageSet.
// Handles: asset names, SF Symbols, base64 data, and light/dark image pairs.
// Used by all feature handlers that accept image configurations.
// See: docs/carplay-api-surface.md §4 (CPImageSet), §2 (map button images), etc.
